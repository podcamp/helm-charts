{
  "name": "template-base",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint:commit": "commitlint --from=HEAD~1"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.5"
  }
}